[build]
builder = "nixpacks"

[build.nixpacks]
pkgs = ["python313"]

[deploy]
startCommand = "pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
